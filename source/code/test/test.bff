.Project =
[
    .Name = 'test'
    .Kind = .Kind_ConsoleApp
    .Group = 'Tests'

    .BaseDir = '$WorkspaceCodeDir$/test'

    .CopyModules = true

    .Private =
    [
        .DependsOn =
        [
            .Runtime = {
                'iceshard'
                'asset_module'
                'debug_module'
                'sdl2_driver'
            }
#if exists(VULKAN_SDK)
            .Runtime + 'vulkan_driver'
#endif
        ]

        .Modules = {
            'imgui'
            'SDL2'
            'Assimp'
        }
        .Uses = {
            'engine'
            'engine_debug'
            'application'
        }
    ]
]
.Projects + .Project

.Project =
[
    .Name = 'test'
    .Kind = .Kind_ConsoleApp
    .Group = 'Tests'

    .BaseDir = '$WorkspaceCodeDir$/test'

    .CopyModules = true

    .Private =
    [
        .DependsOn =
        [
            .Runtime = {
                'iceshard'
                'asset_module'
                'sdl2_driver'
                'imgui_driver'
            }
#if exists(VULKAN_SDK)
            .Runtime + 'vulkan_driver'
#endif
        ]

        .Modules = {
            'glm'
            'imgui'
            'SDL2'
            'Assimp'
        }
        .Uses = {
            'debugui_system'
            'application'
            'engine'
        }
    ]
]
.Projects + .Project
